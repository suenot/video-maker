[pytest]
markers =
    network: tests that hit a network endpoint (e.g. edge-tts)
addopts = -m "not network"
; Default `pytest tests/` run is hermetic and deselects network tests.
; To run the network tests deliberately: ./venv/bin/python3 -m pytest -m network -v
